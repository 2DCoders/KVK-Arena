Assets for the KVK Arena UI

Place static images, icons, and other public assets here. Files in this folder are served from `/assets/*` at runtime, for example `/assets/logo.svg`.

Guidelines:
- Keep images optimized for web (small file sizes).
- Prefer SVG for logos and simple illustrations.
- Use `hero.svg` as a placeholder; replace with production art later.
